function independentFunctions(name, breed){
    function wakeDog(name, breed){
        console.log(`Wake ${name} the ${breed}`);
    }
    // function leashDog(name, breed){
    //     console.log(`Leash ${name} the ${breed}`)
    // }
}